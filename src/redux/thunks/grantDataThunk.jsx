import { supabase } from "@/utils/supabase/supabase";

import {setGrantUserData, setGrantRideData, setGrantChallengeData, setGrantSurveyData, clearGrantData} from "../slices/grantDataSlice"

export const getGrantData = () => async (dispatch) => {
    try {

    } catch (error) {

    }
    
}